{
  "devDependencies": {
    "parcel": "^1.11.0"
  },
  "dependencies": {
    "typescript": "^3.2.4"
  },
  "scripts": {
    "build": "yarn parcel build functions/handler.ts --target=node --global handler -o index.js --bundle-node-modules --no-source-maps"
  }
}
